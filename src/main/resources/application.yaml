spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mi_base_de_datos
    username: postgres
    password: mimaridoronal
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
jwt:
  secret: your_secret_key